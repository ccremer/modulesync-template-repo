name: Sync

on:
  push:
    branches:
      - main
      - master

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Configure Git
        run: |
          git config --global user.name "$GITHUB_ACTOR"
          git config --global user.email "$GITHUB_ACTOR@users.noreply.github.com"
      - name: Install Ruby 3.0
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.0'
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.5.3
        with:
            ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
      - name: Install modulesync
        run: bundle install
      - name: Run modulesync
        env:
          GITHUB_TOKEN: ${{ secrets.PAT }}
        run: bundle exec msync update --pr --pr-labels dependency --pr-title "modulesync update" --pr-target-branch "main" --remote-branch "modulesync/$(git rev-parse --short HEAD)"

      # - name: Run update
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.PAT }}
      #   run: docker run --rm -it -u $(id -u) -e GITHUB_TOKEN
      #    --env SSH_AUTH_SOCK=/tmp/ssh_agent.sock --volume "${SSH_AUTH_SOCK}:/tmp/ssh_agent.sock"
      #    --volume "${HOME}/.ssh/known_hosts:/home/msync/.ssh/known_hosts:ro"
      #    --volume "${HOME}/.gitconfig:/home/msync/.gitconfig:ro"
      #    -v ${PWD}:/app vshn/modulesync
      #    msync update --pr --pr-labels dependency --pr-title "modulesync update" --pr-target-branch "main" --remote-branch "modulesync"
